{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main open-codesign window. Shell execution is intentionally not enabled until a reviewed Node sidecar boundary exists.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
